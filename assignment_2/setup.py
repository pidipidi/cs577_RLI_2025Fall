from setuptools import setup

setup(
    name="assignment_2",
    version="0.0.1",
    install_requires=['gymnasium', 'numpy'],
    # entry_points are typically not needed for standard env registration
)
